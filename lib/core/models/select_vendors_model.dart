import 'package:digital_recipts/core/models/vendors_model.dart';

class SelectVendorsModel {
  final VendorsData vendorsData;
  bool isSelected;

  SelectVendorsModel(this.vendorsData, this.isSelected);
}